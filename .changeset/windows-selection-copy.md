---
"@sma1lboy/rove": patch
---

On Windows, Ctrl+C copies a Rove terminal selection and clears its highlight without interrupting the embedded engine. With no selection, Ctrl+C still reaches the engine normally.
