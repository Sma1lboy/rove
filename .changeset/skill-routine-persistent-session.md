---
"@sma1lboy/rove": patch
---

The Rove agent skill's routine reference now documents `--persistent-session` (one standing task, every firing a new turn), `--prompt-file`, and the `revived` / `deferred` run statuses the binary already reports. Skill version 40.
