---
"@sma1lboy/rove": patch
---

Derive which group a task is in — waiting-on-you, landing, ready-for-review, working, idle — from signals Rove already stores, instead of trusting the declared `status` a crashed worker leaves behind.
