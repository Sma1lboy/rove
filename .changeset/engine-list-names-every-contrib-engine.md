---
"@sma1lboy/rove": patch
---

`rove api engine-list` help now names every shipped contrib engine, Devin and Qoder CLI included, because it reads them from the engine catalog instead of a hand-kept list; the API, quick-start, and plugin-authoring docs list both too.
