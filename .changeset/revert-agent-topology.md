---
"@sma1lboy/rove": patch
---

Revert the Agent Topology page (#758). The panel, its `ctrl+a` `4` rail row, the `communications` task field, and the `task.recordCommunication` API verb are withdrawn pending a product decision on the multi-agent view.
