---
"@sma1lboy/rove": patch
---

Kanban cards breathe: the title and date rows no longer sit flush against the card border. Padding is now uniform on all four sides, and the column drops its own inter-card gap in exchange, so a card costs one extra row rather than two and the board keeps its dense two-line grammar.
