---
"@sma1lboy/rove": patch
---

Add short demo GIFs for the SDK example plugins (hello-events, settings-demo, turn-notify) under `docs/assets/plugins/`, referenced from `docs/PLUGIN-AUTHORING.md`. Each example gains a reproducible `demo.tape` recording script.
