---
"@sma1lboy/rove": patch
---

Forward mouse clicks to the app inside a terminal tab when it enabled mouse tracking. Claude Code's expandable tool rows ("Read 2 files…") now open on click inside Rove, the same as in a bare terminal. Shift+click/drag keeps Rove's own text selection over a mouse-aware app.
