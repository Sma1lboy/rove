---
"@sma1lboy/rove": patch
---

Rove skill v30: document that peer `send` messages can carry file paths — screenshots, logs, and diffs are shared via the filesystem and opened with the receiver's Read tool.
