---
"@sma1lboy/rove": patch
---

Release notes drop the "Thanks @user! -" preamble — each patch line now ends with the author credit instead ("summary — @user"), via a local changelog wrapper around @changesets/changelog-github.
