---
"@sma1lboy/rove": patch
---

Add an interactive Rove TUI mock to the landing page. It reproduces the product's own layout — grouped task cards carrying their terminal tabs, the workspace tab strip, side-by-side panes, and the quota/key-hint status bar — and picking a task in the rail swaps every pane. Keyboard-reachable and translated in both languages.
