---
"@sma1lboy/rove": patch
---

The sidebar marks an engine waiting on your answer (a question or a permission prompt) with an amber `?`, matching the tab strip and Inbox, instead of the red `!` used for failures. Off macOS, Claude's `⏸ plan mode on` line no longer renders as a colored emoji in the terminal pane.
