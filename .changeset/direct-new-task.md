---
"@sma1lboy/rove": patch
---

Open New task with `ctrl+n` from non-input panes and workspace pages.

The sidebar keeps its bare `n` shortcut. Engine composers and embedded shell
terminals keep receiving `ctrl+n` for readline/emacs next-history instead of
opening the dialog.
