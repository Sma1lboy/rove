---
"@sma1lboy/rove": patch
---

`h` and `l` switch buttons in the confirm dialogs, alongside the arrow keys. The delete prompt — and every other yes/no dialog built on `DialogConfirm`, plus Run again — took only ←/→, so a two-button choice was the one place in the rail your hand had to leave the home row. The new-chat dialog and the Settings level switch already answered to h/l; these are the dialogs that had been left out. Nothing else changes: the arrows still work, and these dialogs carry no text field for the letters to belong to instead.
